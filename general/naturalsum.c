#include<stdio.h>
int main()
{
  int i,n,sum;
  sum=0;
  printf("enter the value of n:");
  scanf("%d",&n);
  for(n==0;sum+n;n++)
	  printf("sum of the %d numbers:%d\n",n,sum);
	 scanf("%d%d",&n,&sum);
  return 0;
}
